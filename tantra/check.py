Input: list of times + UVs
Output: A valid schedule exists

sol 1: recursive

def check_times(schedules):
    times = []
    for each time in the UV:
        times.add(time)
        ok = check_ok(times)
        if ok:
            add other times
        

#input ideal
[times]
cour = [uv, jour, heures, ..]

def find_all_posible_schedule(all_times):
    times = []
    if all_forced_times_present(times, all_times) and is_valid(times):
        return times


